RALPH: Redesign BentoRegistry

== Iteration 1 ==
File changed: app/(home)/components/sections/bento-registry.tsx (single file)

User direction (via AskUserQuestion notes):
- Keep the bento GRID (NOT a flat row).
- Add an install command to EVERY card.
- Previews must autoplay + loop (NOT hover-to-play).

Implemented:
US-001 autoplay+loop:
  - <Player> now uses `autoPlay loop` (verified autoPlay?: boolean in @remotion/player Player.d.ts:24).
  - Removed hover-to-play gating: deleted handleEnter/handleLeave, playerRef, playing state,
    togglePlay useCallback, and the mobile sm:hidden play/pause button.
  - Kept spotlight overlay (now z-10, pointer-events-none) and whileHover y:-4 lift — only
    hover-to-PLAY removed, not hover polish.

US-002 install command per card:
  - New InstallPill({name}) component inside the file. Copies full
    `npx shadcn@latest add remocn/${name}` to clipboard; displays abbreviated
    `npx shadcn add remocn/<name>` for space. Mono font, truncate, $ prefix, Copy->Check feedback.
  - Fires trackEvent("install_command_copied", {component: name, surface: "bento",
    package_manager: "npm"}) — matches existing install-command.tsx convention.

US-003 grid preserved:
  - Asymmetric bento intact: grid-cols-3 grid-rows-2 with featured col-span-2 row-span-2,
    plus second 2-col grid. All 5 components present.
  - Card body is now flex flex-col; install pill pinned to bottom via mt-auto so the tall
    featured card aligns the command to the footer.

Patterns/learnings:
  - Reusable copy patterns live at app/(home)/components/install-command.tsx and
    components/docs/copy-button.tsx. Install command canonical format from config/landing.ts
    INSTALL_COMMAND = "npx shadcn@latest add remocn/<name>".
  - Registry keyed by name in registry/__index__ (entry.Component, entry.config.{durationInFrames,fps,compositionWidth,compositionHeight}).
  - User preference: do NOT run build/dev/verify (handled by user). LSP server not installed here,
    so verification was static (type-def check + read).

Status: all 3 stories passes:true. Pending reviewer verification.
