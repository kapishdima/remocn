# Ralph progress — hero player → glass-code-block with registry-usage code

## US-001 (DONE)
File: app/(home)/components/sections/hero.tsx
- Switched hero player entry: registry["browser-flow"] → registry["glass-code-block"].
- Added module-scope HERO_CODE string passed via inputProps.code. It demonstrates the real remocn flow:
  - comment with the shadcn CLI install: `npx shadcn@latest add remocn/blur-reveal remocn/mesh-gradient-bg`
  - imports of the copied components from @/components/remocn/*
  - composing them in a Remotion scene: <AbsoluteFill><MeshGradientBg /><BlurReveal text="..." /></AbsoluteFill>
- Both referenced components are REAL registry entries (registry/remocn/blur-reveal, mesh-gradient-bg). Verified prop names: BlurReveal requires `text: string` (used); MeshGradientBg props all optional (self-closing OK).
- inputProps: dropped browser-flow-only `url`; pass title "LaunchScene.tsx", width 860, height 480, and background = "linear-gradient(rgba(5,5,8,0.5), rgba(5,5,8,0.62)), url(/bg.jpg) center / cover no-repeat" — keeps the bg.jpg texture behind the glass card (continuity with previous task) with a darker wash so code stays legible through the glass.
- Analytics component labels updated browser-flow → glass-code-block (preview_played/paused).

## Sizing math (fits 1280x720 canvas, no overflow)
- glass-code-block config: durationInFrames 180, fps 30, canvas W=1280 H=720 (same as before, so hero preview aspect unchanged).
- Card 860x480 centered. Body height = 480-40(chrome)-40(padding)=400; ~14 lines (one blank) ≈ 387px < 400. Longest line ~68 chars ≈ 653px < ~784px usable. All lines fit.

## Learnings
- glass-code-block uses a simple regex tokenizer + per-line Sequence stagger (staggerFrames default 4). Keywords purple, strings green, comments gray.
- Site renders components from SOURCE via registry/__index__.tsx, so editing inputProps on the landing is enough; no registry rebuild needed for the hero to change.
- Project convention (CLAUDE memory): never run build/dev — user verifies. Verified via grep + source inspection + architect.
