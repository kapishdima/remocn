# Ralph progress — remove AI-slop eyebrow styling (uppercase + wide letter-spacing)

## US-001 (DONE)
The classic AI eyebrow (uppercase + tracking-[0.18em] mono) appeared in 4 places. Removed
`uppercase` and `tracking-[0.18em]` from each, keeping margin/size/color:
- app/(home)/components/section-heading.tsx:43  (shared SectionHeading eyebrow — covers bento, get-started, landing-partners, sponsors wall-of-love)
- app/(home)/components/sections/works-with.tsx:10
- app/(home)/sponsors/components/sections/hero.tsx:17
- app/(home)/sponsors/components/sections/wall-of-love.tsx:56 (SponsorGroup label)

Result class: `font-mono text-xs font-medium text-muted-foreground` (normal case, normal spacing).

## Verification (grep)
- `uppercase` under app/(home): 0.
- wide letter-spacing (tracking-[..em] / tracking-wide/wider/widest / CSS letter-spacing / inline letterSpacing / textTransform): 0.
- `tracking-tight` (negative heading kerning — intended, NOT slop) preserved across 9 files.

## Learnings
- Eyebrow strings are already title/sentence case ("Get started", "The registry", "Sponsors", "Wall of love", "Built on tools you already use", "It's just React"), so dropping the CSS `uppercase` renders them correctly with no string edits.
- Distinguish negative `tracking-tight` (good, tightens large headings) from positive wide tracking (the AI-slop tell). Only the latter was removed.
- Project convention (CLAUDE memory): never run build/dev — user verifies. Verified via grep + architect.
