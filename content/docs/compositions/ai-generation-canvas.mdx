---
title: AI Generation Canvas
description: A prompt input morphs into a dashboard header, then skeletons flip to reveal generated content.
---

A four-phase composition that tells the story of AI generation in one shot. The prompt types into a centered input, the input springs upward and stretches into a header bar, brand-tinted skeleton cards fade in beneath it, then each card flips on its Y axis to reveal the rendered dashboard content with a staggered cascade.

<ComponentPreview name="ai-generation-canvas" />

## Installation

<InstallBlock name="ai-generation-canvas" />

## Usage

```tsx
import { AIGenerationCanvas } from "@/components/remocn/ai-generation-canvas";

<AIGenerationCanvas
  prompt="Generate a dashboard"
  accentColor="#7c3aed"
  cardCount={4}
/>
```

## Props

<PropsTable
  rows={[
    {
      name: "prompt",
      type: "string",
      default: '"Generate a dashboard"',
      description: "Text typed into the input during the first phase.",
    },
    {
      name: "accentColor",
      type: "string",
      default: '"#7c3aed"',
      description: "Brand color used for the input border, shimmer tint, and chart accents.",
    },
    {
      name: "cardCount",
      type: "number",
      default: "4",
      description: "Number of skeleton cards rendered and revealed in the dashboard grid.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container.",
    },
  ]}
/>

## Notes

<Note title="Skeletons should not feel like loading spinners">
  The shimmer is intentionally tinted with the accent color at low alpha rather than the usual neutral gray. That single detail reframes the wait as anticipation — it reads as the model thinking, not as the network stalling.
</Note>
