---
title: Compositions
description: Full scenes built by composing primitives.
---

> 🚧 Coming soon.

## Roadmap

- **`StaggeredBentoGrid`** — cards animate in one after another using `<Series>` / `<Sequence from={index * 15}>`.
- **`ChatToPreviewLayout`** — split-screen where the chat shrinks while a preview pane expands.
- **`BoundingBoxSelector`** — Figma-style selection rectangle around `children`.
