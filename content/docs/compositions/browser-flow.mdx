---
title: Browser Flow
description: A full Safari/Chrome simulation from URL typing to scroll and click
---

<ComponentPreview name="browser-flow" />

## Installation

<InstallBlock name="browser-flow" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { BrowserFlow } from "@/components/remocn/browser-flow";

const BrowserFlowScene = () => (
  <BrowserFlow url="remocn.dev" />
);

export const RemotionRoot = () => (
  <Composition
    id="BrowserFlow"
    component={BrowserFlowScene}
    durationInFrames={270}
    fps={30}
    width={1280}
    height={720}
  />
);
```

### With Backdrop

Renders transparent — supply the background via `Backdrop`:

```tsx
import { Backdrop } from "@/components/remocn/backdrop";
import { BrowserFlow } from "@/components/remocn/browser-flow";

<Backdrop fill={{ type: "gradient", value: "radial-gradient(ellipse at center, #11141c 0%, #050505 75%)" }}>
  <BrowserFlow url="remocn.dev" />
</Backdrop>
```

## Props

<PropsTable
  rows={[
    {
      name: "url",
      type: "string",
      default: '"remocn.dev"',
      description: "The URL typed into the address bar and used in the autocomplete suggestion",
    },
    {
      name: "speed",
      type: "number",
      default: "1",
      description: "Multiplier applied to the current frame to slow down or speed up the entire sequence",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container",
    },
  ]}
/>
