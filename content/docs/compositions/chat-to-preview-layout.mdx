---
title: Chat to Preview Layout
description: Two-column layout where the chat shrinks and the preview expands.
---

A composition that animates the `flex-basis` of two side-by-side panels, simulating a chat interface that hands off real estate to a live preview. Both panels use `overflow: hidden` so their content doesn't break the layout while the widths animate.

<ComponentPreview name="chat-to-preview-layout" />

## Installation

<InstallBlock name="chat-to-preview-layout" />

## Usage

```tsx
import { ChatToPreviewLayout } from "@/components/remocn/chat-to-preview-layout";

<ChatToPreviewLayout
  chat={<MyChat />}
  preview={<MyPreview />}
  startChatRatio={0.5}
  endChatRatio={0.25}
/>
```

## Props

<PropsTable
  rows={[
    {
      name: "chat",
      type: "ReactNode",
      description: "The chat panel. Falls back to a sample chat mockup.",
    },
    {
      name: "preview",
      type: "ReactNode",
      description: "The preview panel. Falls back to a sample landing page mockup.",
    },
    {
      name: "startChatRatio",
      type: "number",
      default: "0.5",
      description: "Initial fraction of width given to the chat panel (0..1).",
    },
    {
      name: "endChatRatio",
      type: "number",
      default: "0.25",
      description: "Final fraction of width given to the chat panel (0..1).",
    },
    {
      name: "background",
      type: "string",
      default: '"#0a0a0a"',
      description: "Outer background color.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container.",
    },
  ]}
/>

## Notes

<Note title="Why flex-basis is OK here">
  Most Remotion animations should stick to `transform`/`opacity`. This component is an exception because the resizing of two panels *is* the effect — there's no way to fake it with `transform`. Keep both child panels' contents inside an `overflow: hidden` wrapper to avoid reflow artifacts.
</Note>
