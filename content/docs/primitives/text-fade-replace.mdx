---
title: Text Fade Replace
description: Cross-fade between two strings on the same line without layout shift.
---

A typography primitive that swaps one string for another in place. Both spans are absolutely positioned in a relative container so they never push each other in the DOM during the fade.

<ComponentPreview name="text-fade-replace" />

## Installation

<InstallBlock name="text-fade-replace" />

## Usage

```tsx
import { TextFadeReplace } from "@/components/remocn/text-fade-replace";

<TextFadeReplace from="Before" to="After" fontSize={72} />
```

## Props

<PropsTable
  rows={[
    {
      name: "from",
      type: "string",
      required: true,
      description: "Initial text shown at the start of the composition.",
    },
    {
      name: "to",
      type: "string",
      required: true,
      description: "Replacement text shown at the end of the composition.",
    },
    {
      name: "fontSize",
      type: "number",
      default: "48",
      description: "Font size in pixels.",
    },
    {
      name: "fontWeight",
      type: "number",
      default: "600",
      description: "CSS font-weight.",
    },
    {
      name: "color",
      type: "string",
      default: "\"#171717\"",
      description: "Text color for both strings.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to both spans.",
    },
  ]}
/>

## Notes

<Note title="Absolute positioning">
  Both texts are `position: absolute` inside a zero-size relative anchor. This guarantees there is no layout reflow during the cross-fade, even when `from` and `to` have different widths.
</Note>

<Warning title="Long strings">
  If your strings are very long, make sure your composition is wide enough — neither span will wrap because they use `whiteSpace: nowrap`.
</Warning>
