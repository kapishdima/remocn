---
title: Toast Notification
description: System-style toast that pops in, holds, then slides out
---

<ComponentPreview name="toast-notification" />

## Installation

<InstallBlock name="toast-notification" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { ToastNotification } from "@/components/remocn/toast-notification";

const ToastNotificationScene = () => (
  <ToastNotification
    title="Deployment successful"
    message="Your changes are live at remocn.dev"
    variant="success"
  />
);

export const RemotionRoot = () => (
  <Composition
    id="ToastNotification"
    component={ToastNotificationScene}
    durationInFrames={90}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "title",
      type: "string",
      default: "\"Deployment successful\"",
      description: "Bold headline shown at the top of the toast",
    },
    {
      name: "message",
      type: "string",
      default: "\"Your changes are live at remocn.dev\"",
      description: "Secondary message shown below the title",
    },
    {
      name: "variant",
      type: "\"success\" | \"error\" | \"info\" | \"warning\"",
      default: "\"success\"",
      description: "Controls the inline icon and accent color",
    },
    {
      name: "background",
      type: "string",
      default: "\"#fafafa\"",
      description: "Frame background color",
    },
    {
      name: "cardColor",
      type: "string",
      default: "\"white\"",
      description: "Toast card background color",
    },
    {
      name: "textColor",
      type: "string",
      default: "\"#171717\"",
      description: "Color of the toast title",
    },
    {
      name: "mutedColor",
      type: "string",
      default: "\"#71717a\"",
      description: "Color of the toast message",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the toast card",
    },
  ]}
/>
