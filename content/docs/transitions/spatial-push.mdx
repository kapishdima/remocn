---
title: Spatial Push
description: A new scene physically presses the old one back into the frame
---

<ComponentPreview name="spatial-push" />

## Installation

<InstallBlock name="spatial-push" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { SpatialPush } from "@/components/remocn/spatial-push";

const SpatialPushScene = () => (
  <SpatialPush
    from={<SceneA />
);

export const RemotionRoot = () => (
  <Composition
    id="SpatialPush"
    component={SpatialPushScene}
    durationInFrames={90}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "from",
      type: "ReactNode",
      description: "The outgoing scene. Falls back to a labeled colored panel.",
    },
    {
      name: "to",
      type: "ReactNode",
      description: "The incoming scene. Falls back to a labeled colored panel.",
    },
    {
      name: "direction",
      type: '"up" | "down" | "left" | "right"',
      default: '"up"',
      description: "Direction the incoming scene enters from",
    },
    {
      name: "transitionStart",
      type: "number",
      default: "durationInFrames * 0.4",
      description: "Frame at which the push begins",
    },
    {
      name: "transitionDuration",
      type: "number",
      default: "30",
      description: "Easing window for Scene A's retreat, in frames",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container",
    },
  ]}
/>
