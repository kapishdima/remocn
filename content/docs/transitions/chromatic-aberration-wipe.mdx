---
title: Chromatic Aberration Wipe
description: An ultra-fast slide transition with an RGB glitch on the peak frames.
---

A high-energy transition built for DevTools demos and product reels. Two scenes slide past each other in just 5–8 frames, and on the middle of that window the entire container gets a red+cyan `drop-shadow` filter to fake RGB channel separation. The glitch is intentionally short — restraint is the whole point. Three to five frames is enough for the viewer's eye to register speed without ever consciously parsing the colors.

<ComponentPreview name="chromatic-aberration-wipe" />

## Installation

<InstallBlock name="chromatic-aberration-wipe" />

## Usage

```tsx
import { ChromaticAberrationWipe } from "@/components/remocn/chromatic-aberration-wipe";

<ChromaticAberrationWipe
  from={<SceneC />}
  to={<SceneB />}
  direction="left"
  transitionDuration={7}
  aberrationOffset={8}
/>
```

## Props

<PropsTable
  rows={[
    {
      name: "from",
      type: "ReactNode",
      description: "The outgoing scene. Falls back to a labeled colored panel.",
    },
    {
      name: "to",
      type: "ReactNode",
      description: "The incoming scene. Falls back to a labeled colored panel.",
    },
    {
      name: "direction",
      type: '"left" | "right"',
      default: '"left"',
      description: "Direction the outgoing scene slides toward.",
    },
    {
      name: "transitionStart",
      type: "number",
      default: "durationInFrames * 0.4",
      description: "Frame at which the wipe begins.",
    },
    {
      name: "transitionDuration",
      type: "number",
      default: "7",
      description: "Length of the wipe in frames. Keep it short (5–8) for the glitch to land.",
    },
    {
      name: "aberrationOffset",
      type: "number",
      default: "8",
      description: "Horizontal offset in pixels for the red and cyan drop shadows during the peak frames.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container.",
    },
  ]}
/>

<Warning title="Less is more">
  The chromatic aberration should only flash for 3–5 frames at peak speed. Any longer and the viewer starts consciously reading the red and cyan fringes — which kills the illusion of raw velocity. Trust the subconscious; pull the filter the moment the slide decelerates.
</Warning>
