---
title: Workflows
description: Multi-step process visualizations.
---

> 🚧 Coming soon.

## Roadmap

- **`ProgressSteps`** / **`Timeline`** — sequential step indicator, fills the connecting line via `stroke-dashoffset`.
- **`SuccessConfetti`** — particle burst rendered to `<canvas>`, seeded with `random()` from `@remotion/random` for deterministic playback.
