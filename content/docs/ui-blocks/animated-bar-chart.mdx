---
title: Animated Bar Chart
description: Bars spring up from the baseline with a staggered cascade.
---

<ComponentPreview name="animated-bar-chart" />

A data-viz primitive that renders SVG `<rect>` bars and animates each one with a `spring()` driven `scaleY` transform. Each bar is offset by `staggerFrames`, producing a clean cascade.


## Installation

<InstallBlock name="animated-bar-chart" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { AnimatedBarChart } from "@/components/remocn/animated-bar-chart";

const AnimatedBarChartScene = () => (
  <AnimatedBarChart
    data={[35, 60, 45, 80, 55, 70, 90, 65]}
    barColor="#0ea5e9"
  />
);

export const RemotionRoot = () => (
  <Composition
    id="AnimatedBarChart"
    component={AnimatedBarChartScene}
    durationInFrames={90}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "data",
      type: "number[]",
      default: "[35, 60, 45, 80, 55, 70, 90, 65]",
      description: "Bar values. Auto-scaled to the available height.",
    },
    {
      name: "labels",
      type: "string[]",
      description: "Optional labels rendered under each bar.",
    },
    {
      name: "width",
      type: "number",
      default: "1000",
      description: "SVG viewBox width.",
    },
    {
      name: "height",
      type: "number",
      default: "500",
      description: "SVG viewBox height.",
    },
    {
      name: "barColor",
      type: "string",
      default: "\"#0ea5e9\"",
      description: "Fill color used for every bar.",
    },
    {
      name: "background",
      type: "string",
      default: "\"#0a0a0a\"",
      description: "Outer background color.",
    },
    {
      name: "gap",
      type: "number",
      default: "16",
      description: "Pixel gap between bars.",
    },
    {
      name: "staggerFrames",
      type: "number",
      default: "6",
      description: "Frame delay applied to each successive bar.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the outer container.",
    },
  ]}
/>

## Notes

<Note title="Transform origin">
  Each bar uses `transformOrigin: bottom` so the spring animation grows from the baseline rather than the center. We rely on `transformBox: fill-box` to scope the origin to the rect's own box.
</Note>
