---
title: Code Diff Wipe
description: Reveal an "after" code snippet by wiping a "before" snippet away.
---

A dev-block that stacks two code panes and animates a vertical wipe from left to right. The top "before" layer is masked with `clip-path: inset(0 X% 0 0)` where `X` interpolates from `0` to `100`, revealing the "after" code beneath. An optional handle marker rides the wipe line.

<ComponentPreview name="code-diff-wipe" />

## Installation

<InstallBlock name="code-diff-wipe" />

## Usage

```tsx
import { CodeDiffWipe } from "@/components/remocn/code-diff-wipe";

<CodeDiffWipe
  before={"function get() { /* old */ }"}
  after={"const get = async () => { /* new */ }"}
/>
```

## Props

<PropsTable
  rows={[
    {
      name: "before",
      type: "string",
      description: "Multi-line code shown on top, wiped away by the animation.",
    },
    {
      name: "after",
      type: "string",
      description: "Multi-line code revealed underneath as the wipe progresses.",
    },
    {
      name: "language",
      type: "string",
      default: "\"tsx\"",
      description: "Language label shown in the pane header.",
    },
    {
      name: "background",
      type: "string",
      default: "\"#0a0a0a\"",
      description: "Editor background color.",
    },
    {
      name: "accent",
      type: "string",
      default: "\"#0ea5e9\"",
      description: "Accent color for the wipe line, handle, and \"after\" label.",
    },
    {
      name: "transitionStart",
      type: "number",
      default: "20",
      description: "Frame at which the wipe begins.",
    },
    {
      name: "transitionDuration",
      type: "number",
      default: "60",
      description: "Number of frames the wipe takes from 0% to 100%.",
    },
    {
      name: "showHandle",
      type: "boolean",
      default: "true",
      description: "Render a circular handle on the wipe line.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the outer container.",
    },
  ]}
/>

## Notes

<Note title="GPU friendly">
  `clip-path` is composited on the GPU in modern browsers, so the wipe stays cheap even with long code blocks.
</Note>

<Warning title="Long lines">
  The pane uses `whiteSpace: pre` and `overflow: hidden`, so very long lines get clipped rather than wrapped. Pre-format your snippets if you need them to fit.
</Warning>
