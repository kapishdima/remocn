---
title: Dynamic Split Screen
description: Cinematic two-column split where one panel shrinks and the other expands into view
---

<ComponentPreview name="chat-to-preview-layout" />

## Installation

<InstallBlock name="chat-to-preview-layout" />

## Usage

```tsx
// src/Root.tsx
import { Composition } from "remotion";
import { ChatToPreviewLayout } from "@/components/remocn/chat-to-preview-layout";

const ChatToPreviewLayoutScene = () => (
  <ChatToPreviewLayout
    chat={<MyChat />
);

export const RemotionRoot = () => (
  <Composition
    id="ChatToPreviewLayout"
    component={ChatToPreviewLayoutScene}
    durationInFrames={120}
    fps={30}
    width={1280}
    height={720}
  />
);
```

## Props

<PropsTable
  rows={[
    {
      name: "chat",
      type: "ReactNode",
      description: "The chat panel. Falls back to a sample chat mockup.",
    },
    {
      name: "preview",
      type: "ReactNode",
      description: "The preview panel. Falls back to a sample landing page mockup.",
    },
    {
      name: "startChatRatio",
      type: "number",
      default: "0.5",
      description: "Initial fraction of width given to the chat panel (0..1)",
    },
    {
      name: "endChatRatio",
      type: "number",
      default: "0.25",
      description: "Final fraction of width given to the chat panel (0..1)",
    },
    {
      name: "background",
      type: "string",
      default: '"#0a0a0a"',
      description: "Outer background color",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className for the outer container",
    },
  ]}
/>
