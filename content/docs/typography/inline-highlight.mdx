---
title: Inline Highlight
description: Animate one word inside a sentence from a base color to a brand color.
---

A typography primitive that splits a sentence into `before`, `highlight`, and `after` parts and animates the color of the middle word using `interpolateColors` from Remotion.

<ComponentPreview name="inline-highlight" />

## Installation

<InstallBlock name="inline-highlight" />

## Usage

```tsx
import { InlineHighlight } from "@/components/remocn/inline-highlight";

<InlineHighlight
  before="Ship faster with "
  highlight="remocn"
  highlightColor="#ff5e3a"
  fontSize={72}
/>
```

## Props

<PropsTable
  rows={[
    {
      name: "before",
      type: "string",
      required: true,
      description: "Text rendered before the highlighted word.",
    },
    {
      name: "highlight",
      type: "string",
      required: true,
      description: "The word that animates color.",
    },
    {
      name: "after",
      type: "string",
      default: "\"\"",
      description: "Text rendered after the highlighted word.",
    },
    {
      name: "baseColor",
      type: "string",
      default: "\"#171717\"",
      description: "Starting color of the highlighted word and color of the surrounding text.",
    },
    {
      name: "highlightColor",
      type: "string",
      default: "\"#ff5e3a\"",
      description: "Final color of the highlighted word.",
    },
    {
      name: "fontSize",
      type: "number",
      default: "48",
      description: "Font size in pixels.",
    },
    {
      name: "fontWeight",
      type: "number",
      default: "600",
      description: "CSS font-weight.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the underlying span.",
    },
  ]}
/>

## Notes

<Note title="Smooth color blending">
  `interpolateColors` blends in RGB space, so transitions between contrasting hues stay smooth without going through muddy intermediate colors.
</Note>
