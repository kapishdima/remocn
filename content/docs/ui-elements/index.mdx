---
title: UI Elements
description: Reusable UI primitives that act like animated React components.
---

> 🚧 Coming soon.

## Roadmap

- **`SpringPopIn`** — elastic scale-in with overshoot, backed by `spring()`.
- **`SimulatedCursor`** — cursor that moves between target points and clicks.
- **`PulsingIndicator`** — looping `Math.sin()`-driven pulse for "loading" affordances.
