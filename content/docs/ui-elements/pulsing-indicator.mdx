---
title: Pulsing Indicator
description: Continuous pulsing dot for loading and "live" states.
---

A tiny UI primitive for signalling activity. An inner dot breathes via `Math.sin(frame)` mapped to `scale` and `opacity`, while an outer ring "pings" outward on a separate phase.

<ComponentPreview name="pulsing-indicator" />

## Installation

<InstallBlock name="pulsing-indicator" />

## Usage

```tsx
import { PulsingIndicator } from "@/components/remocn/pulsing-indicator";

<PulsingIndicator color="#22c55e" size={16} speed={8} />
```

## Props

<PropsTable
  rows={[
    {
      name: "color",
      type: "string",
      default: "\"#22c55e\"",
      description: "Dot and ring color.",
    },
    {
      name: "size",
      type: "number",
      default: "16",
      description: "Dot diameter in pixels. The ring expands beyond this.",
    },
    {
      name: "speed",
      type: "number",
      default: "8",
      description: "Lower = faster pulse. The value is the frame divisor inside Math.sin.",
    },
    {
      name: "background",
      type: "string",
      default: "\"white\"",
      description: "Background color of the surrounding container.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the outer container.",
    },
  ]}
/>

## Notes

<Note title="Deterministic pulse">
  Animation is driven by `Math.sin(frame / speed)` — no `setInterval`, no randomness, fully frame-accurate on every render.
</Note>
