---
title: Spotlight Card
description: A card with a moonlight-cool radial spotlight that follows a synthetic cursor and lights up its microborder.
---

A bento-style card primitive in the dark. A virtual cursor traces a slow Lissajous figure-8 across the card surface; under it, a wide radial gradient softly lights the inside while a brighter, narrower gradient bleeds through a 1px wrapper to pick out the microborder. The cursor lags by exactly one frame so the highlight feels organic instead of robotic.

<ComponentPreview name="spotlight-card" />

## Installation

<InstallBlock name="spotlight-card" />

## Usage

```tsx
import { SpotlightCard } from "@/components/remocn/spotlight-card";

<SpotlightCard title="Your feature" body="Two-line description goes here." />
```

## Props

<PropsTable
  rows={[
    {
      name: "title",
      type: "string",
      default: "\"Spotlight Card\"",
      description: "Heading shown when no children are provided.",
    },
    {
      name: "body",
      type: "string",
      default: "\"Soft radial light follows the cursor, picking out the microborder.\"",
      description: "Body copy shown when no children are provided.",
    },
    {
      name: "cardWidth",
      type: "number",
      default: "520",
      description: "Card width in pixels.",
    },
    {
      name: "cardHeight",
      type: "number",
      default: "320",
      description: "Card height in pixels.",
    },
    {
      name: "glowSize",
      type: "number",
      default: "600",
      description: "Diameter (px) of the radial spotlight.",
    },
    {
      name: "glowOpacity",
      type: "number",
      default: "0.08",
      description: "Maximum alpha of the surface glow. Keep this low to stay lunar.",
    },
    {
      name: "background",
      type: "string",
      default: "\"#050505\"",
      description: "Page background color around the card.",
    },
    {
      name: "cardColor",
      type: "string",
      default: "\"#0a0a0a\"",
      description: "Card surface color.",
    },
    {
      name: "textColor",
      type: "string",
      default: "\"#fafafa\"",
      description: "Heading color.",
    },
    {
      name: "mutedColor",
      type: "string",
      default: "\"#71717a\"",
      description: "Body copy color.",
    },
    {
      name: "speed",
      type: "number",
      default: "1",
      description: "Playback speed multiplier.",
    },
    {
      name: "className",
      type: "string",
      description: "Optional className passed to the root container.",
    },
    {
      name: "children",
      type: "ReactNode",
      description: "Override the default title/body slot with custom content.",
    },
  ]}
/>

## Notes

<Note title="Keep the light cold">
  Use white or near-white glow at very low opacity (0.05–0.1). Brand-colored glow tends to read as cheap when amplified by the radial gradient.
</Note>
